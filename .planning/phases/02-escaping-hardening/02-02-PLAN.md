---
phase: 02-escaping-hardening
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "package.json"
  - "package-lock.json"
  - "tsconfig.json"
  - "src/taskpane/safeInsertion.ts"
  - "tests/safeInsertion.test.ts"
autonomous: true
requirements: [ESCAPE-01, ESCAPE-02]

must_haves:
  truths:
    - "src/taskpane/safeInsertion.ts is the only new module in openclerk-word, and its exported functions' parameters are branded SafeHyperlinkUrl/SafeHtml types, not plain string -- verified by a transient compile-time negative check, not merely by convention"
    - "insertSafeHyperlink's fallback order (insertHyperlink -> insertHtml -> insertText) is fixed and deterministic, covered by dedicated dispatch-order tests (lifted spec-less probe item: ordering)"
    - "toSafeHyperlinkUrl/toSafeHtml, re-exported through openclerk-core, resolve correctly when imported from openclerk-word's own node_modules, not just inside openclerk-core's own test suite"
  artifacts:
    - path: "src/taskpane/safeInsertion.ts"
      provides: "insertSafeHyperlink/insertSafeComment -- the sole wrapper functions this repo allows to call raw Office.js insertion APIs"
    - path: "tests/safeInsertion.test.ts"
      provides: "Dedicated unit tests for the wrapper module (D-06)"
  key_links:
    - "safeInsertion.ts imports SafeHtml/SafeHyperlinkUrl from openclerk-core@^0.3.0 -- must resolve to the newly-published version, not the old 0.2.x range which lacks these exports"
    - "insertSafeHyperlink's insertHyperlink branch retains the pre-existing type-widening cast, since @types/office-js has no declaration for this Office.js API (Pitfall 2) -- removing the cast would break compilation, not add safety"
  prohibitions:
    - "safeInsertion.ts's wrapper functions must not re-implement escaping/URL-validation logic locally -- its only import from openclerk-core is the two branded types, and it must only ever consume already-branded values, never produce them."
---

<objective>
Bump openclerk-word's dependency on the now-published `openclerk-core@0.3.0` (Plan 01), then build
`src/taskpane/safeInsertion.ts` -- the single wrapper module that will own every raw Office.js
`insertHtml`/`insertHyperlink`/`insertComment` call in this repo, with branded-type-only exported
function parameters. Add dedicated unit tests (D-06).

Purpose: ESCAPE-02 requires a single wrapper module owning all raw insertion calls; ESCAPE-01
requires that module's parameters to be type-constrained to the branded types just published.
Output: `src/taskpane/safeInsertion.ts` (new), `tests/safeInsertion.test.ts` (new), openclerk-word's
`package.json` dependency bumped to `openclerk-core ^0.3.0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-escaping-hardening/02-CONTEXT.md
@.planning/phases/02-escaping-hardening/02-RESEARCH.md
@.planning/phases/02-escaping-hardening/02-PATTERNS.md
</context>

<interfaces>
openclerk-core@0.3.0's new export surface (Plan 01 -- type-only for the branded types, values for
the constructors; confirm against the actual installed package before use, do not assume):

`export type SafeHyperlinkUrl = string & { readonly __brand: "SafeHyperlinkUrl" };`
`export type SafeHtml = string & { readonly __brand: "SafeHtml" };`
`export function toSafeHyperlinkUrl(url: string): SafeHyperlinkUrl | null;` -- returns null on
invalid/unsafe input, never throws.
`export function toSafeHtml(raw: string): SafeHtml;` -- never returns null.

Existing word.ts logic this plan's Task 2 ports into safeInsertion.ts (read word.ts directly before
writing -- these line numbers are a pointer, not a substitute for reading the current file):
- The hyperlink-insertion dispatch function, currently at word.ts lines 217-233: feature-detects a
  newer Office.js hyperlink API first, falls back to an HTML-insertion API, falls back to plain-text
  insertion as a last resort, then syncs the Word.RequestContext once.
- The comment-insertion call site, currently at word.ts lines 1229-1233, inside a Word.run block.
</interfaces>

## Artifacts This Phase Produces

This plan creates two new files in openclerk-word: `src/taskpane/safeInsertion.ts` (exports
`insertSafeHyperlink`, `insertSafeComment`) and `tests/safeInsertion.test.ts`. It also bumps
`package.json`'s `dependencies.openclerk-core` to `^0.3.0` and `package-lock.json` accordingly, and
adds `"tools"` to `tsconfig.json`'s `exclude` array (a small, necessary fix so a project-wide
`tsc --noEmit` type-check -- this phase's actual compile-time verification mechanism -- does not
choke on the unrelated standalone `tools/pdf-extract` subproject). No files in `openclerk-core` are
touched by this plan (that repo's changes are Plan 01's, already published).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump openclerk-core to ^0.3.0, verify branded exports resolve, scope tsc to exclude tools/</name>
  <files>package.json, package-lock.json, tsconfig.json</files>
  <read_first>package.json (current `dependencies.openclerk-core` line); tsconfig.json (current `exclude` array); node_modules/openclerk-core/package.json (pre-bump state, for comparison)</read_first>
  <action>
    Add `"tools"` to tsconfig.json's `exclude` array (currently `["node_modules", "dist", "lib",
    "lib-amd"]`) so a project-wide TypeScript check does not pull in `tools/pdf-extract` -- a
    separately-built, separately-`node_modules`'d standalone tool (per ARCHITECTURE.md, already
    documented as decoupled from the main add-in build). Without this exclusion, a whole-project
    `tsc --noEmit` reports pre-existing, unrelated type-declaration errors from that subproject's
    own dependencies, which would make a project-wide compile-time check for this phase's
    branded-type guarantee unusable for every remaining plan.
    Edit `dependencies.openclerk-core` in package.json from `"^0.2.6"` to `"^0.3.0"`.
    Run `npm install` (updates package-lock.json's resolved/integrity entry for openclerk-core).
    Confirm the installed openclerk-core package's version is 0.3.0 or higher and that its
    emitted type-declaration file exports the four new identifiers from Plan 01.
    Confirm openclerk-core still resolves from the public npm registry (not a git/tarball/local
    override) by inspecting package-lock.json's resolved field for the openclerk-core entry --
    mirrors Phase 1's T-01-01/T-01-SC supply-chain check.
  </action>
  <acceptance_criteria>
    grep -A6 '"exclude"' tsconfig.json includes "tools"; npx tsc --noEmit exits 0 against the
    current baseline (confirms the exclusion alone is sufficient, before this plan's own
    safeInsertion.ts exists); grep '"openclerk-core"' package.json shows ^0.3.0; node -p
    "require('./node_modules/openclerk-core/package.json').version" prints 0.3.0 or higher; grep
    -c "toSafeHyperlinkUrl\|toSafeHtml\|SafeHyperlinkUrl\|SafeHtml"
    node_modules/openclerk-core/lib/index.d.ts is at least 1; grep -A2 '"openclerk-core"'
    package-lock.json shows a resolved URL starting with https://registry.npmjs.org/openclerk-core/.
  </acceptance_criteria>
  <verify>
    <automated>npm install && npx tsc --noEmit && npm ls openclerk-core</automated>
  </verify>
  <done>openclerk-word depends on openclerk-core ^0.3.0 (resolved from the public npm registry, exposing the four new branded-type exports), and a project-wide `npx tsc --noEmit` is now a usable verification command for the rest of this phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/taskpane/safeInsertion.ts -- the sole wrapper for raw Office.js insertion calls</name>
  <files>src/taskpane/safeInsertion.ts</files>
  <read_first>src/taskpane/word.ts (the current hyperlink-insertion dispatch function at lines 217-233, and the current comment-insertion call site at lines 1229-1233 -- used only as the logic reference for this new file, not modified here); node_modules/openclerk-core/lib/index.d.ts (Task 1 -- the actual installed branded-type export names/shapes)</read_first>
  <behavior>
    - insertSafeHyperlink(context, item, url: SafeHyperlinkUrl, displayText: SafeHtml) prefers a
      feature-detected newer hyperlink-insertion method on `item` when present
    - falls back to a feature-detected HTML-insertion method on `item` when the first is absent,
      building the anchor markup directly from the already-branded url/displayText (no further
      escaping step -- the inputs are already validated/escaped by construction)
    - falls back to `item`'s plain-text insertion method when neither of the above exist
    - all three branches converge on exactly one `await context.sync()` call, made after the
      dispatch decision, not before
    - insertSafeComment(context, range, text: SafeHtml) calls `range`'s comment-insertion method
      with `text`, then awaits `context.sync()` exactly once
  </behavior>
  <action>
    Create src/taskpane/safeInsertion.ts with the same `/// <reference types="office-js" />` and
    `/* global Word */`-style header comments word.ts's own header uses, since this file also
    references Word.* globals.
    Add a type-only import of `SafeHtml, SafeHyperlinkUrl` from "openclerk-core" -- this must be
    the ONLY import from "openclerk-core" in this file; no function (value) import from that
    package belongs here, since this module only ever consumes already-branded values, never
    produces them.
    Implement `export async function insertSafeHyperlink(context: Word.RequestContext, item:
    Word.Range, url: SafeHyperlinkUrl, displayText: SafeHtml): Promise<void>`, porting the 3-tier
    fallback dispatch described in behavior above from word.ts's current hyperlink-insertion
    dispatch function (lines 217-233) -- omit the additional escaping step that function currently
    performs in its HTML-insertion branch, since url/displayText arrive here already branded.
    This function owns its own `context.sync()` call internally, exactly once, standardizing on
    the calling convention that function already uses (resolving the two-pattern inconsistency
    PATTERNS.md flagged between it and the current comment-insertion call site).
    Implement `export async function insertSafeComment(context: Word.RequestContext, range:
    Word.Range, text: SafeHtml): Promise<void>`, calling range's comment-insertion method with
    text, then awaiting context.sync() internally -- the same internally-owned-sync convention as
    insertSafeHyperlink, so both exported functions in this file follow one consistent API shape.
    Precede both exported functions with a JSDoc-style comment (matching this project's
    rationale-focused comment convention) explaining that branded parameter types, not plain
    strings, are what makes this file's insertion guarantee compiler-enforced -- this file is the
    only place in openclerk-word allowed to call a raw Office.js insertion API, per the ESLint
    guard a later plan in this phase adds.
    Preserve the existing type-widening cast (to bridge the newer hyperlink-insertion API, which
    has no declaration in the installed @types/office-js) exactly as the current code already
    does -- this is a pre-existing, unavoidable exception in the code being ported, not new risk
    introduced by this refactor (RESEARCH.md Pitfall 2).
    As a one-time verification of the branding guarantee (not a permanent file): after writing the
    module, create a scratch file at src/taskpane/__brand-check.ts containing a single line that
    assigns a plain string literal to a variable typed `import("openclerk-core").SafeHyperlinkUrl`,
    run the project-wide type check, confirm it reports an error (the brand intersection genuinely
    rejects a bare string, proving this is a real compile-time gate, not a cosmetic alias), then
    delete the scratch file and re-run the type check to confirm it is clean again.
  </action>
  <acceptance_criteria>
    grep -n "export async function insertSafeHyperlink" src/taskpane/safeInsertion.ts is found,
    with a `url: SafeHyperlinkUrl` and `displayText: SafeHtml` parameter (not `string`); grep -n
    "export async function insertSafeComment" src/taskpane/safeInsertion.ts is found, with a
    `text: SafeHtml` parameter; grep -A2 'from "openclerk-core"' src/taskpane/safeInsertion.ts
    shows exactly one import line naming only SafeHtml and SafeHyperlinkUrl (no other
    identifiers); the transient brand-check scratch file reports a type error when present and is
    deleted afterward (test ! -f src/taskpane/__brand-check.ts); npx tsc --noEmit exits 0 once the
    scratch file is removed.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && printf 'const bad: import("openclerk-core").SafeHyperlinkUrl = "x";\n' > src/taskpane/__brand-check.ts && (! npx tsc --noEmit) && rm -f src/taskpane/__brand-check.ts && npx tsc --noEmit</automated>
  </verify>
  <done>safeInsertion.ts exports insertSafeHyperlink/insertSafeComment with branded-type-only parameters that genuinely reject a bare string at compile time (verified via a transient negative check), preserves the existing 3-tier hyperlink fallback and comment-insertion behavior, and imports nothing but the two branded types from openclerk-core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/safeInsertion.test.ts</name>
  <files>tests/safeInsertion.test.ts</files>
  <read_first>C:\Users\willi\openclerk-core\tests\utils.test.ts (describe/test style analog -- single-quoted strings, one behavior per test, no mocking library); src/taskpane/safeInsertion.ts (Task 2's actual final exported signatures -- read the file just written, do not assume)</read_first>
  <behavior>
    - toSafeHyperlinkUrl imported from openclerk-core resolves correctly from this repo's own
      node_modules and rejects an unsafe scheme, accepts an https URL
    - insertSafeHyperlink calls only the hyperlink-insertion method when a mock object exposes all
      three duck-typed methods (it takes priority over the other two)
    - insertSafeHyperlink calls only the HTML-insertion method when a mock object exposes it and
      the plain-text method but not the hyperlink-insertion method
    - insertSafeHyperlink calls only the plain-text method when a mock object exposes none of the
      other two
    - every insertSafeHyperlink case calls the mock context's sync function exactly once
    - insertSafeComment calls the mock range's comment-insertion function with the branded text
      and calls the mock context's sync function exactly once
  </behavior>
  <action>
    Create tests/safeInsertion.test.ts importing `toSafeHyperlinkUrl, toSafeHtml` from
    "openclerk-core" and `insertSafeHyperlink, insertSafeComment` from
    "../src/taskpane/safeInsertion".
    Add a describe block for toSafeHyperlinkUrl with the two sanity cases from behavior above --
    deliberately small and non-duplicative of openclerk-core's own (Plan 01 Task 2) coverage;
    its purpose here is confirming the re-export resolves and behaves correctly from
    openclerk-word's own node_modules, not re-proving every validation edge case.
    Add a describe block for insertSafeHyperlink dispatch with three test cases, each using a
    plain object literal shaped like a Word.Range with jest.fn() stand-ins for the three
    duck-typed insertion methods, omitting the irrelevant method(s) per case to force the fallback
    branch under test (no Word.js mock library -- plain duck-typed objects, consistent with this
    project's testing conventions), plus a mock context object with a jest.fn() sync method
    resolving to undefined. Assert only the expected method was called (and the others were not),
    and that the mock context's sync function was called exactly once, for all three cases.
    Add a describe block for insertSafeComment asserting the mock range's comment-insertion
    function was called with the branded text and the mock context's sync function was called
    exactly once.
  </action>
  <acceptance_criteria>
    npx jest tests/safeInsertion.test.ts -t "toSafeHyperlinkUrl" passes; npx jest
    tests/safeInsertion.test.ts -t "dispatch" passes with all three fallback-order cases present
    (grep -c "test(" tests/safeInsertion.test.ts is at least 6 across all describe blocks); npx
    jest tests/safeInsertion.test.ts -t "insertSafeComment" passes.
  </acceptance_criteria>
  <verify>
    <automated>npx jest tests/safeInsertion.test.ts</automated>
  </verify>
  <done>tests/safeInsertion.test.ts passes, covering smart-constructor re-export sanity, the full 3-tier dispatch fallback order (lifted spec-less probe item: ordering), and insertSafeComment -- closing D-06's dedicated-test-coverage requirement for this module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safeInsertion.ts's exported function signatures -> raw Office.js insertion API | The actual compile-time enforcement point -- only a branded value can reach a raw insertion call from here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | safeInsertion.ts's exported function signatures | high | mitigate | Both exported functions take only SafeHyperlinkUrl/SafeHtml parameters, never `string`; Task 2's transient negative compile-check proves a bare string is genuinely rejected, not merely aliased. Maps to RESEARCH.md's Security Domain / Known Threat Patterns table (V5 Input Validation). |
| T-2-09 | Tampering | safeInsertion.ts re-implementing validation locally | medium | mitigate | Task 2's action forbids local escaping/validation logic and the single-import acceptance criterion enforces it structurally (only SafeHtml/SafeHyperlinkUrl imported from openclerk-core, no function imports) -- occurs at the primary insertion trust boundary, so mitigated rather than accepted even at medium severity. |
</threat_model>

<verification>
Post-execution: openclerk-word depends on openclerk-core ^0.3.0 resolved from the public npm
registry; src/taskpane/safeInsertion.ts exports insertSafeHyperlink/insertSafeComment with
branded-type-only parameters, a transiently-verified compile-time guarantee, and dedicated passing
tests covering the fallback dispatch order and comment insertion.
</verification>

<success_criteria>
Roadmap Phase 2 Success Criterion 1 is now fully satisfied (branded types exist and type-constrain
the insertion helper's parameters). ESCAPE-02's wrapper module now exists, though word.ts's own
call sites have not yet migrated to it (Plan 03).
</success_criteria>

<output>
Create `.planning/phases/02-escaping-hardening/02-02-SUMMARY.md` when done
</output>
