---
phase: 02-escaping-hardening
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - "eslint.config.mjs"
  - ".eslintrc.json"
  - ".github/workflows/ci.yml"
autonomous: true
requirements: [ESCAPE-03]

must_haves:
  truths:
    - "eslint.config.mjs exists at the project root (not .eslintrc.json) with a no-restricted-syntax rule banning insertHtml/insertHyperlink/insertComment method calls everywhere except an exact files: [\"**/safeInsertion.ts\"] exemption (lifted spec-less probe item: ESCAPE-03 unclassified -- resolved as 'the rule is genuinely wired into npm run lint, verified by deliberately reintroducing and then reverting a violation')"
    - "npm run lint passes against the now-fully-migrated codebase, and fails (non-zero exit, with the configured message) when a raw insertHtml/insertHyperlink/insertComment call is deliberately reintroduced outside safeInsertion.ts"
    - "CI runs npm run lint as a genuinely blocking job (no continue-on-error, no output-suppressing fallback) that also gates the publish job, so a PR that reintroduces a bypass cannot reach a green state or a release"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "The no-restricted-syntax bypass-guard + safeInsertion.ts exemption"
    - path: ".github/workflows/ci.yml"
      provides: "New lint job (D-05), gating publish"
  key_links:
    - "office-addin-lint's npm run lint invocation resolves eslint.config.mjs at the project root over its own bundled default (RESEARCH.md Pitfall 1) -- verified empirically against the installed tooling during planning, not assumed"
    - "eslint.config.mjs's files: [\"**/safeInsertion.ts\"] override matches exactly src/taskpane/safeInsertion.ts and no other file"
  prohibitions:
    - "The no-restricted-syntax bypass-guard must be defined in eslint.config.mjs (not .eslintrc.json), and its files exemption glob must match only safeInsertion.ts, not a broader directory -- a scoping mistake in either direction silently defeats the guard while appearing correct in code review."
    - "The new CI lint job must actually fail the workflow run when npm run lint exits non-zero -- no continue-on-error and no output-suppressing fallback."
---

<objective>
Add a project-root `eslint.config.mjs` with a `no-restricted-syntax` rule banning raw
`insertHtml`/`insertHyperlink`/`insertComment` method calls everywhere except
`src/taskpane/safeInsertion.ts`, retire the dead `.eslintrc.json`, add a blocking `lint` job to CI
(D-05), and machine-prove the guard actually rejects a deliberately-reintroduced violation before
confirming ESCAPE-03 complete.

Purpose: ESCAPE-03 requires ESLint to fail the build on any bypass of the wrapper module. Per
RESEARCH.md's highest-confidence finding, this repo's `npm run lint` (via `office-addin-lint`)
never reads `.eslintrc.json` at all -- only a project-root `eslint.config.mjs` -- so this is the
one file placement that actually works, verified directly against the installed tooling source.
Output: `eslint.config.mjs` (new), `.eslintrc.json` (removed), `.github/workflows/ci.yml` with a
new blocking `lint` job that also gates `publish`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-escaping-hardening/02-CONTEXT.md
@.planning/phases/02-escaping-hardening/02-RESEARCH.md
@.planning/phases/02-escaping-hardening/02-PATTERNS.md
</context>

<interfaces>
Verified directly against this repo's installed `node_modules/office-addin-lint/lib/lint.js`
during planning (not assumed from RESEARCH.md alone): `getEsLintBaseCommand` resolves
`path.resolve(process.cwd(), "eslint.config.mjs")` and uses it if it exists via `fs.existsSync`,
otherwise falls back to `office-addin-lint`'s own bundled config. The lint file glob (from
`node_modules/office-addin-lint/lib/defaults.js`) is `src/**/*.{ts,tsx,js,jsx}`, which already
covers `src/taskpane/safeInsertion.ts` and `src/taskpane/word.ts` with no extra config needed.

Also verified: `require('eslint-plugin-office-addins').configs.recommended` is an array (flat
config format), so `...officeAddins.configs.recommended` is the correct spread syntax, and
`@typescript-eslint/parser` already resolves as an installed transitive dependency -- no new
`package.json` dependency is needed for this plan.
</interfaces>

## Artifacts This Phase Produces

This plan creates `eslint.config.mjs` at the project root, deletes `.eslintrc.json`, and adds a
new `lint:` job to `.github/workflows/ci.yml` (also adding `lint` to the `publish:` job's `needs`
array). No `src/` files are touched.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create eslint.config.mjs, delete the dead .eslintrc.json</name>
  <files>eslint.config.mjs, .eslintrc.json</files>
  <read_first>.eslintrc.json (current, to be removed); package.json (the "lint" script -- confirms it runs `office-addin-lint check`, not a bare eslint invocation)</read_first>
  <action>
    Delete `.eslintrc.json` -- confirmed dead for `npm run lint` purposes (RESEARCH.md Pitfall 1,
    verified directly against the installed `office-addin-lint` source during planning); leaving
    it in place risks a future developer editing the wrong file and believing the bypass-guard is
    active when it is not.
    Create `eslint.config.mjs` at the project root (the exact filename `office-addin-lint`'s
    `lint.js` hardcodes -- not `eslint.config.js`, not `.ts`). Import `eslint-plugin-office-addins`
    (default import) and `@typescript-eslint/parser`. Export a flat-config array: first spread
    `officeAddins.configs.recommended` (so the existing office-addin-specific rules keep firing);
    then a config object with `plugins: { "office-addins": officeAddins }`, `languageOptions: {
    parser: tsParser }`, and `rules: { "no-restricted-syntax": ["error", ...] }` containing exactly
    three selector entries in ONE array (not split across separate config objects -- flat config's
    `no-restricted-syntax` does not merge across matching entries, per RESEARCH.md's citation of
    eslint/eslint#19239):
    - `CallExpression[callee.property.name='insertHtml']`, message "Raw insertHtml calls must go
      through src/taskpane/safeInsertion.ts."
    - `CallExpression[callee.property.name='insertHyperlink']`, message "Raw insertHyperlink calls
      must go through src/taskpane/safeInsertion.ts."
    - `CallExpression[callee.property.name='insertComment']`, message "Raw insertComment calls
      must go through src/taskpane/safeInsertion.ts."
    Then add a final config object scoped to `files: ["**/safeInsertion.ts"]` setting `rules: {
    "no-restricted-syntax": "off" }` -- this exact glob, not a broader directory glob, so the
    exemption does not silently cover any other file under `src/taskpane/`.
  </action>
  <acceptance_criteria>
    test ! -f .eslintrc.json (deleted); test -f eslint.config.mjs; grep -c
    "callee.property.name='insertHtml'" eslint.config.mjs equals 1; grep -c
    "callee.property.name='insertHyperlink'" eslint.config.mjs equals 1; grep -c
    "callee.property.name='insertComment'" eslint.config.mjs equals 1; grep -c
    'files: \["\*\*/safeInsertion.ts"\]' eslint.config.mjs equals 1 (exact scoped glob, not a
    broader directory); npm run lint exits 0 against the current (fully-migrated) codebase.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>eslint.config.mjs exists at the project root with the no-restricted-syntax bypass-guard and an exact safeInsertion.ts-only exemption; the dead .eslintrc.json is removed; npm run lint passes against the fully-migrated codebase.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the lint CI job (D-05) and gate publish on it</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml (the existing test: job, lines 53-71, as the structural template; the publish: job's needs array)</read_first>
  <action>
    Insert a new `lint:` job immediately after the existing `test:` job (before `installer-smoke:`),
    mirroring `test:`'s exact step structure (checkout, setup-node@v4 with node-version '18' and
    npm cache, `npm ci`, then a final step running `npm run lint` instead of `npm test`), with
    `needs: build` matching `test:`'s dependency. Do not add `continue-on-error` or any other
    non-blocking flag to this job or its steps -- the job must genuinely fail the workflow run
    when `npm run lint` exits non-zero.
    Add `lint` to the `publish:` job's `needs` array (currently `[build, test, installer-smoke,
    installer-smoke-macos, offline-smoke]`), so a broken lint state can never reach the publish
    step -- consistent with D-05's stated purpose that a bad PR should not go green.
  </action>
  <acceptance_criteria>
    A Node script that loads .github/workflows/ci.yml via js-yaml and asserts: jobs.lint exists;
    jobs.lint.needs equals "build"; jobs.lint.steps contains a step whose run field equals exactly
    "npm run lint"; no step or job-level continue-on-error key is set anywhere on jobs.lint or its
    steps; jobs.publish.needs includes "lint" -- reports OK only if every one of these holds.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const y=require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8')); const j=y.jobs.lint; const ok = j && j.needs==='build' && j.steps.some(s=>s.run==='npm run lint') && !j['continue-on-error'] && !j.steps.some(s=>s['continue-on-error']) && y.jobs.publish.needs.includes('lint'); if(!ok){console.error('FAIL'); process.exit(1);} console.log('OK')"</automated>
  </verify>
  <done>ci.yml has a new lint job (structurally identical to the existing test job, running npm run lint, no continue-on-error) that also gates the publish job.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Machine-prove the bypass-guard rejects a deliberately-reintroduced violation, then revert</name>
  <files>src/taskpane/__lint-verify-scratch.ts (transient, created and deleted within this task)</files>
  <read_first>src/taskpane/safeInsertion.ts (confirm it is correctly exempted by Task 1's config); src/taskpane/word.ts (post-Plan-03 state -- confirm the current clean baseline this task's first check depends on)</read_first>
  <action>
    Run `npm run lint` first and confirm it exits 0 (clean baseline -- word.ts was already
    migrated in Plan 03, safeInsertion.ts is correctly exempted by Task 1's config).
    Create a scratch file at src/taskpane/__lint-verify-scratch.ts containing a single exported
    function that accepts a parameter shaped like `{ insertHtml: (html: string, loc: unknown) =>
    void }` and calls its `insertHtml` method directly -- this is RESEARCH.md's own documented
    "deliberately reintroduce a violation" verification step for Pitfall 1 (a security gate that
    appears to exist but doesn't enforce anything), applied here as a one-time machine-proof rather
    than a permanent test.
    Run `npm run lint` again and confirm it now exits non-zero and its output contains the exact
    configured message for the insertHtml selector.
    Delete the scratch file and re-run `npm run lint`, confirming it exits 0 again (clean).
  </action>
  <acceptance_criteria>
    The first npm run lint (pre-scratch-file) exits 0; npm run lint with the scratch file present
    exits non-zero and its output contains "insertHtml calls must go through"; test ! -f
    src/taskpane/__lint-verify-scratch.ts after cleanup; the final npm run lint (post-cleanup)
    exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && printf 'export function scratchLintCheck(x: { insertHtml: (h: string, l: unknown) => void }): void { x.insertHtml("&lt;a&gt;&lt;/a&gt;", null); }\n' > src/taskpane/__lint-verify-scratch.ts && (! npm run lint) && rm -f src/taskpane/__lint-verify-scratch.ts && npm run lint</automated>
  </verify>
  <done>npm run lint is confirmed to genuinely reject a raw insertHtml call added outside safeInsertion.ts (not just appear configured to), then the scratch file is removed and the repo is confirmed clean again -- ESCAPE-03's "fails the build on a bypass" claim is machine-proven, not assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any future PR touching src/taskpane/ -> merged main | The bypass-guard's actual enforcement boundary -- must reject a bypass here, not just locally |
| npm run lint's config resolution (eslint.config.mjs vs. bundled default) | The exact mechanism that determines whether the bypass-guard is real or cosmetic (RESEARCH.md Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Tampering | Any future raw Office.js insertion call added outside safeInsertion.ts | high | mitigate | eslint.config.mjs's no-restricted-syntax rule + the new CI lint job (D-05) fail the build; Task 3 machine-proves this by deliberately reintroducing a violation and confirming rejection before reverting. Maps to RESEARCH.md's Known Threat Patterns table ("bypass of the wrapper module" row). |
| T-2-04 | Tampering (of the control itself) | eslint.config.mjs vs. the dead .eslintrc.json | high | mitigate | .eslintrc.json is deleted (it was never actually read by npm run lint, per RESEARCH.md Pitfall 1, verified directly against the installed tooling source during planning); eslint.config.mjs uses the exact filename office-addin-lint's lint.js hardcodes. |
| T-2-10 | Tampering (of the control itself) | CI lint job silently not blocking | medium | mitigate | Task 2's acceptance criteria structurally verify (via a YAML parse, not a visual read) that the lint job has no continue-on-error anywhere and that publish's needs array includes lint. |
</threat_model>

<verification>
Post-execution: `npm run lint` exits 0 against the fully-migrated codebase and exits non-zero
(with the correct message) against a deliberately-reintroduced raw insertion call outside
safeInsertion.ts, confirmed and then reverted; CI's new lint job structurally verified to be
genuinely blocking and to gate publish.
</verification>

<success_criteria>
Roadmap Phase 2 Success Criterion 3 is satisfied: ESLint fails the build (locally via npm run
lint, and in CI via the new blocking lint job) if a raw insertHtml/insertHyperlink/insertComment
call is added outside safeInsertion.ts -- machine-proven against a real, deliberately-introduced
violation, not merely assumed from the config's presence. All four Roadmap Phase 2 Success
Criteria are now satisfied across Plans 01-04.
</success_criteria>

<output>
Create `.planning/phases/02-escaping-hardening/02-04-SUMMARY.md` when done
</output>
