---
phase: 02-escaping-hardening
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "src/taskpane/word.ts"
autonomous: true
requirements: [ESCAPE-01, ESCAPE-02]

must_haves:
  truths:
    - "src/taskpane/word.ts contains zero calls to insertHtml/insertHyperlink/insertComment -- every raw Office.js insertion call now lives exclusively in safeInsertion.ts (lifted spec-less probe item: adjacency -- no raw call is duplicated or left adjacent to the wrapper boundary)"
    - "word.ts contains zero references to the old plain HTML-escaping or URL-validation functions -- all validation now flows through toSafeHtml/toSafeHyperlinkUrl"
    - "Hyperlinking, Bluebook checking, hallucination checking, and opinion-text embedding all still work correctly against a real Word document after the refactor (Roadmap Phase 2 Success Criterion 4)"
  artifacts:
    - path: "src/taskpane/word.ts"
      provides: "All hyperlink/comment insertion workflows migrated to safeInsertion.ts + branded types"
  key_links:
    - "applyCaseLawHyperlinksFromSource / addParentheticalHyperlinks / applyHyperlinksViaProvider -> insertSafeHyperlink(context, item, SafeHyperlinkUrl, SafeHtml) -> safeInsertion.ts's 3-tier dispatch"
    - "parseSourceDocument's citation-map guard -> toSafeHyperlinkUrl(url) !== null -> CitationMap (plain string values, parse-time quality filter only, not the insertion security gate)"
    - "the pincite opinion-text embedding workflow's comment insertion -> insertSafeComment(context, range, SafeHtml) -> safeInsertion.ts"
  prohibitions:
    - "SafeHtml/SafeHyperlinkUrl branding must never be presented or relied upon as evidence that a citation is authentic/non-hallucinated -- branding certifies DOM/hyperlink-insertion safety only, never citation authenticity. In particular, the hallucination-check's nameMismatch guard must remain wired exactly as Phase 1 verified it, untouched by this refactor."
---

<objective>
Migrate every raw Office.js insertion call site and every URL/HTML validation call site in
`src/taskpane/word.ts` onto `safeInsertion.ts`'s wrapper functions and `openclerk-core`'s branded
smart constructors (Plan 02), so word.ts itself no longer calls a raw Office.js insertion API
anywhere, and no longer imports the old plain (unbranded) escaping/validation functions.

Purpose: ESCAPE-02 requires that `safeInsertion.ts` be the ONLY place raw insertion calls exist --
this plan is the actual migration that makes that true, closing the gap between "the wrapper module
exists" (Plan 02) and "nothing bypasses it" (this plan). ESCAPE-01's type-constraint only has teeth
once real call sites are forced through it.
Output: `word.ts` with zero raw `insertHtml`/`insertHyperlink`/`insertComment` calls and zero
imports of the old unbranded validators; all four task-pane workflows verified against a real Word
document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-escaping-hardening/02-CONTEXT.md
@.planning/phases/02-escaping-hardening/02-PATTERNS.md
</context>

<interfaces>
`safeInsertion.ts`'s exported functions (Plan 02 -- confirm against the actual file before use):

`insertSafeHyperlink(context: Word.RequestContext, item: Word.Range, url: SafeHyperlinkUrl,
displayText: SafeHtml): Promise<void>` -- owns its own `context.sync()` call internally.

`insertSafeComment(context: Word.RequestContext, range: Word.Range, text: SafeHtml):
Promise<void>` -- owns its own `context.sync()` call internally.

`openclerk-core`'s smart constructors (Plan 01): `toSafeHyperlinkUrl(url: string):
SafeHyperlinkUrl | null` (returns null on invalid/unsafe input); `toSafeHtml(raw: string):
SafeHtml` (never null).

Exact word.ts call sites this plan migrates (read the actual current file before editing --
line numbers are pointers into the file as last read during planning, not a substitute for
reading the live file):
- Import block: lines 11-31.
- Hyperlink-insertion dispatch function (deleted by Task 1): lines 217-233.
- `applyCaseLawHyperlinksFromSource`: lines 235-291 (citationEntries filter at ~248-251, call
  site at ~282).
- `addParentheticalHyperlinks`: lines 329-377ish (validEntries filter at ~339-343, call site at
  ~366).
- `applyHyperlinksViaProvider`: lines 496-575ish (combined guard at ~552, call site at ~562).
- Comment-insertion call site (inside the pincite opinion-text embedding workflow): lines
  1229-1233.
- `parseSourceDocument`'s citation-map guard: line ~1426.
</interfaces>

## Artifacts This Phase Produces

This plan modifies exactly one existing file, `src/taskpane/word.ts` -- no new files, no new
exported symbols. It deletes the local `applyHyperlinkToItem` function and rewires every call site
that used it or the old unbranded validators onto `safeInsertion.ts` and `openclerk-core`'s branded
smart constructors (both already created in Plan 01/Plan 02).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate imports and the three hyperlink-insertion workflows</name>
  <files>src/taskpane/word.ts</files>
  <read_first>src/taskpane/word.ts (the full current state of the import block, the hyperlink-insertion dispatch function, and all three hyperlink-insertion workflow functions -- read the live file, the line numbers above are pointers only); src/taskpane/safeInsertion.ts (insertSafeHyperlink's exact signature)</read_first>
  <action>
    In the import block (currently lines 11-31): remove `escapeHtml` and `isSafeHyperlinkUrl` from
    the list of names imported from "openclerk-core"; add `toSafeHtml` and `toSafeHyperlinkUrl` in
    their place. Add a new import line immediately after the "openclerk-core" import block:
    `import { insertSafeHyperlink, insertSafeComment } from "./safeInsertion";`.
    <!-- planner-discipline-allow: escapeHtml -->
    <!-- planner-discipline-allow: isSafeHyperlinkUrl -->

    Delete the `applyHyperlinkToItem` function (currently lines 217-233) in its entirety -- its
    logic now lives in safeInsertion.ts's `insertSafeHyperlink` (Plan 02).
    <!-- planner-discipline-allow: applyHyperlinkToItem -->

    In `applyCaseLawHyperlinksFromSource` (currently lines 235-291): restructure the
    `citationEntries` construction (currently a single `.filter(...)` over
    `sourceCitationMap!.entries()`) into a `.map(...)` step producing, per `[citationText,
    rawUrl]` pair, `{ citationText, url: toSafeHyperlinkUrl(rawUrl) }`, followed by a `.filter(...)`
    that drops any entry whose `citationText` is falsy or exceeds `MAX_SEARCH_TEXT_LENGTH`, or
    whose `url` is `null`. Thread the resulting branded `url` value through the `searches` array
    construction, through `matchedItems` (retype its `url` field from `string` to
    `SafeHyperlinkUrl`), and through `candidates` unchanged in structure. Change the final loop's
    insertion call (currently `applyHyperlinkToItem(context, item, url, normalizedText)`) to
    `insertSafeHyperlink(context, item, url, toSafeHtml(normalizedText))`.

    In `addParentheticalHyperlinks` (currently lines 329-377): restructure the `validEntries`
    construction (currently a `.map(...).filter(...)` chain) so the filter step's URL-safety check
    is replaced by mapping each entry's `url` field through `toSafeHyperlinkUrl(entry.url.trim())`
    and filtering out entries whose branded `url` is `null` or whose `citation` length exceeds
    `MAX_SEARCH_TEXT_LENGTH`. Change the final loop's insertion call (currently
    `applyHyperlinkToItem(context, item, entry.url, entry.citation)`) to `insertSafeHyperlink(context,
    item, entry.url, toSafeHtml(entry.citation))`.

    In `applyHyperlinksViaProvider` (currently lines 496-575): replace the combined guard
    (currently `!match || ` a plain URL-safety check on `match.url`) with a two-step check: first
    compute `const safeUrl = match ? toSafeHyperlinkUrl(match.url) : null;`, then guard on `if
    (!safeUrl)` -- preserve the exact same downstream rate-limit-vs-skip branching logic that
    already reads `match`/`provider`, only the URL-validity source changes. Change the final
    loop's insertion call (currently `applyHyperlinkToItem(context, item, match.url, raw)`) to
    `insertSafeHyperlink(context, item, safeUrl, toSafeHtml(raw))`.
  </action>
  <acceptance_criteria>
    grep -c "applyHyperlinkToItem" src/taskpane/word.ts equals 0 (function and all call sites
    removed); grep -c "insertSafeHyperlink" src/taskpane/word.ts equals 4 (1 import line + 3 call
    sites across the three migrated workflows); grep -c "toSafeHyperlinkUrl" src/taskpane/word.ts
    is at least 4 (1 import line + at least 3 usages); grep -c "toSafeHtml" src/taskpane/word.ts
    is at least 4 (1 import line + at least 3 usages); npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>applyHyperlinkToItem is deleted; all three source-hyperlinking workflows (file-based, parenthetical, provider-lookup) call insertSafeHyperlink with branded values instead of the old inline dispatch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate the comment-insertion call site and the parseSourceDocument guard; verify no raw call or old validator remains</name>
  <files>src/taskpane/word.ts</files>
  <read_first>src/taskpane/word.ts (post-Task-1 state -- the comment-insertion call site inside the pincite opinion-text embedding workflow, currently around lines 1229-1233, and parseSourceDocument's citation-map guard, currently around line 1426); .planning/phases/02-escaping-hardening/02-CONTEXT.md (Roadmap Phase 2 Success Criterion 4 -- the manual smoke-test scope)</read_first>
  <action>
    Replace the existing comment-insertion call site (currently at word.ts lines 1229-1233 -- a
    comment explaining Word's native comment UI, then the raw Office.js call on the first
    search-result item passing the embedded-content builder's output as the comment body,
    followed by a standalone context sync) with a single call:
    `insertSafeComment(context, searchResults.items[0], toSafeHtml(buildEmbeddedCommentContent(raw,
    excerpt)))`. Remove the now-redundant standalone sync call immediately after it, since
    `insertSafeComment` owns its own sync internally (Plan 02's wrapper design).

    In `parseSourceDocument`'s citation-map-building guard (currently around line 1426), replace
    the plain URL-safety predicate with `toSafeHyperlinkUrl(url) !== null`. Do NOT change
    `CitationMap`'s value type (stays plain `Map<string, string>`) -- this guard is a parse-time
    quality filter, not the actual insertion security gate (that gate is `toSafeHyperlinkUrl`'s
    re-validation inside `applyCaseLawHyperlinksFromSource`, already migrated in Task 1); threading
    branded types through `CitationMap`/`sourceCitationMap` module-level state is out of this
    phase's scope.
    <!-- planner-discipline-allow: escapeHtml -->
    <!-- planner-discipline-allow: isSafeHyperlinkUrl -->

    Run a whole-file sweep confirming no import of the old plain HTML-escaping or URL-validation
    functions remains, and confirming no direct call remains anywhere in word.ts to the three
    Office.js insertion methods this phase's wrapper module now exclusively owns.
    Run the full Jest suite and the production webpack build to confirm no regressions.
  </action>
  <acceptance_criteria>
    grep -c "escapeHtml\|isSafeHyperlinkUrl" src/taskpane/word.ts equals 0; grep -c
    "\.insertHtml(\|\.insertHyperlink(\|\.insertComment(" src/taskpane/word.ts equals 0; grep -c
    "toSafeHyperlinkUrl" src/taskpane/word.ts equals 5 (1 import + 4 usages: 3 from Task 1's
    workflows + 1 from this task's parseSourceDocument guard); grep -c "insertSafeComment"
    src/taskpane/word.ts equals 2 (1 import + 1 call site); npm test exits 0; npm run build exits
    0.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build</automated>
    <human-check>Open a real Word document (desktop or online) with OpenClerk sideloaded. Run through all four workflows end-to-end: (1) Manage Hyperlinks -- load a source .docx and apply case-law hyperlinks, confirm links are inserted correctly; (2) Bluebook Check -- run a check against document text, confirm issues render; (3) Hallucination Check -- connect a provider (e.g. CourtListener) and run a hallucination scan, confirm the "possible hallucination" guard still renders correctly for a deliberately-mismatched citation; (4) Embed Cited Text -- embed opinion text for a pincite citation, confirm a Word comment is inserted with the expected excerpt. Confirm no workflow regressed after the safeInsertion.ts wrapper refactor (Roadmap Phase 2 Success Criterion 4).</human-check>
  </verify>
  <done>word.ts contains zero raw insertHtml/insertHyperlink/insertComment calls and zero references to the old plain escaping/validation functions; all four task-pane workflows verified working against a real Word document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source .docx OOXML hyperlinks -> word.ts parse -> Word document | Untrusted document content crosses here |
| Provider API lookup results -> word.ts -> Word document hyperlink insertion | Untrusted third-party API response content crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering / Elevation of Privilege | word.ts's 3 hyperlink-insertion workflows | high | mitigate | All 3 workflows now validate every URL via toSafeHyperlinkUrl before it can reach insertSafeHyperlink -- a plain string can no longer compile as the url argument. Maps to RESEARCH.md's Known Threat Patterns table (scheme-injection row). |
| T-2-07 | Tampering | parseSourceDocument's citation-map guard | medium | mitigate | The old plain URL-safety predicate is replaced with toSafeHyperlinkUrl(url) !== null -- same allowlist logic, now sourced from the single canonical smart constructor rather than a second direct call to the deprecated validator. |
| T-2-08 | Repudiation (Core Value regression risk) | Hallucination-check nameMismatch rendering | high | mitigate | Task 2's acceptance criteria (full test suite + build) and the human smoke-test explicitly re-verify the hallucination-check workflow end-to-end after the refactor, mirroring Phase 1's T-01-02 verification -- this refactor must not touch or weaken that guard. |
</threat_model>

<verification>
Post-execution: `grep -c "applyHyperlinkToItem\|escapeHtml\|isSafeHyperlinkUrl" src/taskpane/word.ts`
equals 0; `grep -c "\.insertHtml(\|\.insertHyperlink(\|\.insertComment(" src/taskpane/word.ts`
equals 0; `npm test && npm run build` both exit 0; all four task-pane workflows manually confirmed
working against a real Word document.
</verification>

<success_criteria>
Roadmap Phase 2 Success Criteria 1, 2, and 4 are satisfied: branded types type-constrain the
insertion helper (Plan 01/02), every raw Office.js insertion call in the codebase now lives inside
safeInsertion.ts and nowhere else (this plan closes the loop), and all four task-pane workflows are
confirmed working against a real Word document post-refactor. Criterion 3 (ESLint bypass-guard) is
Plan 04's scope.
</success_criteria>

<output>
Create `.planning/phases/02-escaping-hardening/02-03-SUMMARY.md` when done
</output>
