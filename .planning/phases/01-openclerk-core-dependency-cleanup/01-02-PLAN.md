---
phase: 01-openclerk-core-dependency-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [".planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md", ".planning/codebase/CONCERNS.md"]
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - 'src/commands/ (commands.ts, commands.word.ts, commands.html) contains zero logic duplicating openclerk-core citation-parsing / Bluebook / escaping exports, confirmed by a fresh post-merge grep pass, not merely restated from the pre-merge spot-check already recorded in 01-CONTEXT.md D-03'
    - 'scripts/ contains zero remaining logic duplicating openclerk-core -- scripts/generate-bluebook-data.js, the one file in scripts/ with real overlap, was deleted by PR #33 and is confirmed absent on main'
    - 'no file anywhere in src/ or scripts/ still imports from the deleted local ./providers, ./bluebook, or ./utils paths (from "./providers", from "./bluebook", from "./utils", from "../providers, from "../bluebook, from "../utils patterns all return zero matches)'
    - 'a persistent, dated audit record exists at .planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md documenting the exact grep commands run and their results, so CORE-02 confirmed-none-found is falsifiable and reviewable rather than a verbal-only assertion'
    - '[flagged-unverified assumption per specless-probe-fallback section A] CORE-02 completion criterion is process-based (a manual audit record), not a code-behavior edge case -- the deterministic edge-coverage probe returned unclassified-review-manually for CORE-02; the truths above are the manually-authored substitute verification for that unclassified item, not an automatically-derived edge classification, and are not to be treated as a silently-dropped gap'
  artifacts:
    - '.planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md (new -- CORE-02 audit record)'
    - '.planning/codebase/CONCERNS.md (edited -- openclerk-core duplication tech-debt entry marked resolved)'
  key_links:
    - 'Task 1 / Task 2 grep command output -> Task 3 audit-record file (the literal evidence backing the CORE-02 none-found conclusion)'
    - '.planning/codebase/CONCERNS.md existing openclerk-core logic duplicated, not shared tech-debt entry -> its own Resolved annotation, closing the loop from the concern that originally motivated this phase per 01-CONTEXT.md canonical_refs'
  prohibitions:
    - '[flagged-unverified] The CORE-02 audit must be a fresh post-merge grep pass with recorded literal commands and output, not a rubber-stamped restatement of the pre-merge spot-check already done during gsd-discuss-phase -- D-03 explicitly calls for re-running it after merge.'
    - '[flagged-unverified] If the audit discovers any duplicated logic touching citation-parsing, Bluebook rule application, HTML/hyperlink escaping, or hallucination verification specifically (as opposed to unrelated build/installer tooling), it must be resolved (removed and replaced with an openclerk-core import) or explicitly escalated as a blocker in the plan SUMMARY -- never silently left duplicated when it touches the Core Value guarded paths.'
    - '[flagged-unverified] The audit must not silently skip the discretionary tests/ deep-import spot-check called out in 01-CONTEXT.md Claude Discretion section -- confirm none of the 6 test files PR #33 removes were missed or left with a dangling deep-import.'
---

<objective>
After PR #33 lands (Plan 01-01), formally re-confirm CORE-02: audit src/commands/ and scripts/ for any
logic duplicated in openclerk-core, record the result, and remove/replace anything found. Per D-03 this
is a bounded confirmation of the spot-check already done during /gsd-discuss-phase, not an open-ended
deep-dive -- but it must be a fresh, post-merge, evidence-recorded pass, not a restatement.

Purpose: CORE-02 requires the audit be performed and its result recorded (even if "confirmed: none
found"), so a future contributor does not have to re-derive this confidence from scratch.
Output: A dated audit record (01-DUPLICATION-AUDIT.md) and an updated CONCERNS.md entry marking the
"openclerk-core logic duplicated, not shared" tech debt item resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-openclerk-core-dependency-cleanup/01-CONTEXT.md
@.planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
</context>

## Artifacts This Phase Produces

This plan is subtractive/verification work, not net-new feature code -- no new source symbols
(functions, types, classes) are created in `src/`. The only new artifacts are planning-side records:
`.planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md` (new file) and an
edited entry in `.planning/codebase/CONCERNS.md`. If Task 1 or Task 2 unexpectedly find real
duplicated logic to remove, that removal happens in this plan's execution and must be reflected in
`files_modified` in the SUMMARY -- but per D-03's pre-merge spot-check, no such removal is expected.

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero duplication in src/commands/</name>
  <files>src/commands/commands.ts, src/commands/commands.word.ts, src/commands/commands.html</files>
  <read_first>.planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md (commands.word.ts full-file excerpt, lines 102-134); .planning/phases/01-openclerk-core-dependency-cleanup/01-CONTEXT.md (decision D-03)</read_first>
  <action>
    Read src/commands/commands.ts, src/commands/commands.word.ts, and
    src/commands/commands.html post-merge, and confirm they are unchanged from the pre-merge excerpt
    already captured in 01-PATTERNS.md (PR #33's diff does not touch src/commands/). Grep all three
    files for identifiers that would indicate citation-parsing, Bluebook-rule, or HTML-escaping logic:
    parseCaseCitation, ParsedCitation, BluebookRuleSet, escapeHtml, isSafeHyperlinkUrl,
    citationProviderRegistry, bluebookRuleSetRegistry. Expect zero matches, formally reconfirming D-03's
    spot-check that this directory is unmodified Yeoman/Office-Add-in template boilerplate ("Hello
    World" ribbon command) with no citation/Bluebook logic whatsoever.
  </action>
  <acceptance_criteria>
    grep -rc 'parseCaseCitation\|ParsedCitation\|BluebookRuleSet\|escapeHtml\|isSafeHyperlinkUrl\|citationProviderRegistry\|bluebookRuleSetRegistry' src/commands/ sums to 0 across all three files; src/commands/commands.word.ts still contains only the insertBlueParagraphInWord handler and Office.onReady/Office.actions.associate wiring described in 01-PATTERNS.md, with no new citation-related code added.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "parseCaseCitation\|ParsedCitation\|BluebookRuleSet\|escapeHtml\|isSafeHyperlinkUrl\|citationProviderRegistry\|bluebookRuleSetRegistry" src/commands/</automated>
  </verify>
  <done>src/commands/ formally reconfirmed post-merge to contain zero logic duplicating openclerk-core; D-03's pre-merge spot-check is re-verified with fresh evidence, not just restated.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm zero duplication in scripts/ and zero residual imports of deleted local paths</name>
  <files>scripts/build-docs.js, scripts/convert-logos.js, scripts/install-openclerk.js, scripts/package-release.js, scripts/package-release-offline.js</files>
  <read_first>.planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md (remaining local cross-imports list and audit-task description, lines 135-157); .planning/codebase/STRUCTURE.md (scripts/ directory contents)</read_first>
  <action>
    Confirm scripts/generate-bluebook-data.js no longer exists on main (PR #33 deletes it -- the one
    file in scripts/ with real overlap, per D-03). Grep the remaining scripts/*.js files (build-docs.js,
    convert-logos.js, install-openclerk.js, package-release.js, package-release-offline.js) for the same
    identifier list used in Task 1 (parseCaseCitation, ParsedCitation, BluebookRuleSet, escapeHtml,
    isSafeHyperlinkUrl, citationProviderRegistry, bluebookRuleSetRegistry), expecting zero matches.
    Separately, run a fresh post-merge grep across the entire src/ and scripts/ trees for the import
    patterns from "./providers", from "./bluebook", from "./utils", from "../providers, from
    "../bluebook, from "../utils -- expect zero matches anywhere, since the whole providers/bluebook
    tree and utils.ts were deleted wholesale by PR #33 (01-PATTERNS.md lines 135-157). As the
    discretionary spot-check noted in 01-CONTEXT.md Claude's Discretion, also grep tests/ for any
    remaining deep-import of the now-deleted internal paths (../src/taskpane/providers,
    ../src/taskpane/bluebook, ../src/taskpane/utils) to confirm none of the 6 removed test files
    (bluebook.test.ts, providers.test.ts, utils.test.ts, opinionText.test.ts, hyperlinks.test.ts,
    courtListener.live.test.ts) were missed or left with a dangling import.
  </action>
  <acceptance_criteria>
    test -f scripts/generate-bluebook-data.js exits non-zero (file absent); grep -rc identifier-list across the 5 remaining scripts/*.js files sums to 0; grep -rc 'from "\./providers"\|from "\./bluebook"\|from "\./utils"\|from "\.\./providers\|from "\.\./bluebook\|from "\.\./utils' src/ scripts/ equals 0; grep -rc '\.\./src/taskpane/providers\|\.\./src/taskpane/bluebook\|\.\./src/taskpane/utils' tests/ equals 0.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f scripts/generate-bluebook-data.js && grep -rn "from \"\./providers\"\|from \"\./bluebook\"\|from \"\./utils\"\|from \"\.\./providers\|from \"\.\./bluebook\|from \"\.\./utils" src/ scripts/; grep -rn "\.\./src/taskpane/providers\|\.\./src/taskpane/bluebook\|\.\./src/taskpane/utils" tests/</automated>
  </verify>
  <done>scripts/generate-bluebook-data.js confirmed deleted; no residual imports of the deleted ./providers, ./bluebook, or ./utils paths remain anywhere in src/ or scripts/; none of the 6 removed test files were missed in the discretionary tests/ spot-check.</done>
</task>

<task type="auto">
  <name>Task 3: Write the CORE-02 audit record and close the CONCERNS.md duplication entry</name>
  <files>.planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md, .planning/codebase/CONCERNS.md</files>
  <read_first>.planning/codebase/CONCERNS.md (the "openclerk-core logic duplicated, not shared" tech-debt entry, lines 13-17); the literal grep commands and outputs produced while executing Task 1 and Task 2 in this same plan run</read_first>
  <action>
    Write a new file at .planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md
    recording: today's date; the merge commit SHA from Plan 01-01 Task 2's `git log --merges -1 main`
    output; the exact grep commands run in Task 1 and Task 2 of this plan, each paired with its literal
    match count/output; and an explicit "CORE-02: confirmed -- no logic in src/commands/ or scripts/
    duplicates openclerk-core" conclusion line (or, if Task 1 or Task 2 unexpectedly found something,
    document what was found and how it was resolved instead of that conclusion). Edit
    .planning/codebase/CONCERNS.md's "openclerk-core logic duplicated, not shared" tech-debt entry
    (Tech Debt section) to append a "Resolved" line dated today, referencing the new audit file path
    and the Plan 01-01 merge commit SHA, so the concern that originally motivated this phase is formally
    closed rather than left open indefinitely.
  </action>
  <acceptance_criteria>
    .planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md exists and contains the literal grep commands from Task 1 and Task 2, their match counts, and the string "CORE-02"; .planning/codebase/CONCERNS.md's "openclerk-core logic duplicated, not shared" entry contains a "Resolved" line referencing 01-DUPLICATION-AUDIT.md.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md && grep -c "CORE-02" .planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md && grep -c "Resolved" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>01-DUPLICATION-AUDIT.md created with the full evidence trail from Task 1/Task 2; CONCERNS.md's duplication tech-debt entry marked Resolved -- CORE-02 formally closed per D-03's bounded audit scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/ and src/commands/ build/release tooling -> shipped add-in package | Any duplicated citation/escaping logic here would drift silently from openclerk-core's maintained copy if not caught by this audit |
| Duplication-audit conclusion -> project record (CONCERNS.md / audit file) | Determines whether a future contributor trusts "no duplication exists" without re-verifying it themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | src/commands/ and scripts/ audit findings (CORE-02) | medium | mitigate | Task 1 and Task 2 require the audit to be a fresh post-merge grep pass (not a restatement of the pre-merge spot-check) with literal recorded grep commands and match counts; any citation-parsing/escaping/hallucination-adjacent duplication found must be removed or explicitly escalated, never silently left in place. |
| T-01-05 | Repudiation | Duplication-audit result (CORE-02) not recorded anywhere verifiable | low | mitigate | Task 3 writes a persistent audit record (01-DUPLICATION-AUDIT.md) so "confirmed: none found" is falsifiable/reviewable later, not just an unrecorded verbal claim. |
</threat_model>

<verification>
Post-audit, src/commands/ and scripts/ are confirmed (via recorded grep evidence) to contain zero
logic duplicating openclerk-core; no residual imports of the deleted ./providers, ./bluebook, or
./utils paths remain anywhere in src/, scripts/, or tests/; a dated audit record exists at
.planning/phases/01-openclerk-core-dependency-cleanup/01-DUPLICATION-AUDIT.md; CONCERNS.md's
duplication tech-debt entry is marked resolved.
</verification>

<success_criteria>
CORE-02 is satisfied: src/commands/ and scripts/ have been audited for logic duplicated in
openclerk-core (roadmap success criteria #3), nothing was found beyond what PR #33 already removed
(scripts/generate-bluebook-data.js), and the audit's result is recorded in a persistent, reviewable
artifact rather than left as an unverified claim.
</success_criteria>

<output>
Create `.planning/phases/01-openclerk-core-dependency-cleanup/01-02-SUMMARY.md` when done
</output>
