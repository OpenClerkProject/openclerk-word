---
phase: 01-openclerk-core-dependency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CORE-01]
must_haves:
  truths:
    - 'main contains PR #33 (OpenClerkProject/openclerk-word#33, branch claude/depend-on-openclerk-core) merged via a merge commit, not a squash or rebase, preserving all 4 original commits (D-01) -- verified by git log --merges -1 main and git log --first-parent main -6 --oneline'
    - 'package.json on main has dependencies.openclerk-core equal to ^0.2.6 (npm registry range), not the old git-tag v0.2.1 reference'
    - 'npm ci succeeds with exit code 0 on main post-merge -- the previously-reproduced git-tag / allow-scripts install failure does not recur'
    - 'src/taskpane/word.ts imports checkCitationsForHallucinations, HallucinationCheckResult, citationProviderRegistry, bluebookRuleSetRegistry and related symbols from openclerk-core (not from the deleted ./utils, ./providers, ./bluebook local paths)'
    - 'the nameMismatch rendering branch (Possible hallucination -- {provider} resolves this citation to a different case) is present verbatim in src/taskpane/word.ts post-merge -- the Core Value guard is preserved, not weakened or dropped during merge-conflict resolution'
    - '[flagged-unverified assumption per specless-probe-fallback section A] CORE-01 completion criterion is process-based (PR #33 merge state on GitHub plus resulting main branch state), not a code-behavior edge case -- the deterministic edge-coverage probe returned unclassified-review-manually for CORE-01; the truths listed above are the manually-authored substitute verification for that unclassified item, not an automatically-derived edge classification, and are not to be treated as a silently-dropped gap'
  artifacts: []
  key_links:
    - 'package.json dependencies.openclerk-core -> node_modules/openclerk-core resolved from https://registry.npmjs.org (confirmed via package-lock.json resolved field, not a git/tarball override) -> src/taskpane/word.ts import statement'
    - 'word.ts checkCitationsForHallucinations() call -> HallucinationCheckResult.nameMismatch field -> task-pane DOM status rendering branch (the actual Core Value enforcement path, not just the import)'
    - 'CI workflow (.github/workflows/ci.yml build/test/installer-smoke jobs, npm ci steps) -> green status specifically on the merge commit produced by Task 1 checkpoint (not just the pre-merge PR branch tip that was spot-checked during onboarding)'
  prohibitions:
    - '[flagged-unverified] Merge must not be completed via squash-merge or rebase-merge -- only Create a merge commit preserves the 4 individual commits and commit 3 rationale for the checkCitationsForHallucinations migration, per locked decision D-01. This is not literally forbidden by the roadmap Success Criteria text but would violate an explicit user decision.'
    - '[flagged-unverified] Merge must not be completed by bypassing or overriding a red or pending CI check (e.g. an admin merge override) -- Roadmap Phase 1 Success Criteria item 1 explicitly requires CI green on the landed merge, and GitHub does not enforce this automatically unless branch protection requires it.'
    - '[flagged-unverified] Merge-conflict resolution (if main has moved since PR #33 branch point) must not silently drop or alter the checkCitationsForHallucinations migration or the nameMismatch rendering branch in src/taskpane/word.ts -- this is the project literal Core Value guard per CLAUDE.md and the single highest-severity risk in this phase.'
    - '[flagged-unverified] package-lock.json must resolve openclerk-core to a version genuinely satisfying ^0.2.6 (containing the ReDoS fixes and the caseNamesMatch hallucination-verification-bypass fix) -- a stale or hand-edited lockfile entry could satisfy package.json semver range on paper while still resolving to a pre-fix version.'
    - '[flagged-unverified] openclerk-core must resolve from the public npm registry (registry.npmjs.org) and not from a git URL, local path override, or typosquatted package name -- light supply-chain check appropriate for ASVS L1, even though openclerk-core is a same-org sibling package with an already-reviewed provenance, not a newly-vetted third party.'
---

<objective>
Land PR #33 ("Depend on openclerk-core instead of vendoring its logic") onto main via a human-performed
merge-commit merge, then verify the resulting main branch actually depends cleanly on the published
openclerk-core npm package (CORE-01): npm install succeeds, the dependency pin is correct, and -- most
importantly for this project's Core Value -- the hallucination-check migration onto openclerk-core's
shared checkCitationsForHallucinations did not get silently weakened or dropped during the merge.

Purpose: CORE-01 requires PR #33 merged with CI green and npm install succeeding cleanly against the
openclerk-core ^0.2.6 dependency. Per decision D-02, Claude does not perform the merge itself (no
`gh pr merge`) -- the repo owner merges manually on GitHub. This plan documents that human step as a
checkpoint and automates every verification that follows it.
Output: PR #33 merged to main (human-performed); automated confirmation that the dependency, build,
test suite, and hallucination-check Core Value guard are all intact post-merge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-openclerk-core-dependency-cleanup/01-CONTEXT.md
@.planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md
</context>

## Artifacts This Phase Produces

This plan is verification-only -- it merges an already-open, already-reviewed PR (a human action) and
confirms the result. No new source symbols (functions, types, classes, files) are created by this plan.
Nothing under `src/` is modified by this plan's own tasks; all `src/` changes come from PR #33 itself,
which this plan verifies rather than authors.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Merge PR #33 into main (human action required per D-02)</name>
  <read_first>.planning/phases/01-openclerk-core-dependency-cleanup/01-CONTEXT.md (decisions D-01, D-02)</read_first>
  <action>Merge PR #33 (OpenClerkProject/openclerk-word#33, "Depend on openclerk-core instead of vendoring its logic", branch claude/depend-on-openclerk-core) into main using GitHub's "Create a merge commit" strategy.</action>
  <instructions>
    Per decision D-02 in 01-CONTEXT.md, Claude does not run `gh pr merge` or any other merge automation
    for this PR -- merging is a human-only action for this phase, by explicit user decision, even though
    a CLI/API exists for it.

    1. Open the PR (openclerk-word repo, PR #33) and confirm CI is green on its checks tab before
       merging -- do not merge on a red or pending check.
    2. Use the "Create a merge commit" option -- NOT "Squash and merge" and NOT "Rebase and merge" --
       per decision D-01, to preserve all 4 original commits, including commit 3's rationale for
       migrating checkForHallucinations onto openclerk-core's shared checkCitationsForHallucinations.
    3. Click "Confirm merge".
    4. Locally: git checkout main && git pull.
  </instructions>
  <verification>After you resume, Task 2 runs `git log --merges -1 main` to confirm a merge commit landed and `git log --first-parent main -6 --oneline` to confirm 4 non-merge PR commits are reachable (not squashed into one).</verification>
  <acceptance_criteria>git log --merges -1 main shows a merge commit whose message references PR #33; git log --first-parent main -6 --oneline lists the 4 original PR commits as discrete entries, not one squashed commit.</acceptance_criteria>
  <resume-signal>Reply "merged" once PR #33 is merged via a merge commit and you have pulled main locally. If CI was red, or you used squash/rebase instead of a merge commit, describe what happened instead of replying "merged".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify post-merge dependency, install, build, and CI state (CORE-01)</name>
  <files>package.json, package-lock.json</files>
  <read_first>.planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md (package.json current-state excerpt lines 27-39 and PR #33 diff lines 161-169)</read_first>
  <action>
    After Task 1's checkpoint resolves: run `git log --merges -1 main` and `git log --first-parent main -6 --oneline` to confirm the merge landed as a merge commit with 4 discrete PR commits reachable. Read package.json and confirm dependencies.openclerk-core equals exactly "^0.2.6". Run `npm ci` and confirm it exits 0 with no allow-scripts or resolution error. Run `npm ls openclerk-core` and confirm the resolved version satisfies ^0.2.6, and inspect package-lock.json's "resolved" field for the openclerk-core entry to confirm it is a `https://registry.npmjs.org/openclerk-core/` tarball URL, not a git/tarball/local override (prohibition on the npm supply-chain trust boundary). Run `npm run build` and confirm the production webpack build succeeds. Run `npm test` and confirm all Jest suites pass. If the `gh` CLI is authenticated, run `gh pr checks 33` or `gh run list --branch main --limit 1` to confirm CI is green specifically on the merge commit (not just the pre-merge PR tip that was spot-checked during onboarding); if `gh` is not authenticated, rely on Task 1's checkpoint resume confirmation that CI was green before merge, and note this in the plan SUMMARY rather than blocking on a secondary CI check.
  </action>
  <acceptance_criteria>
    git log --merges -1 main shows a merge commit (not a fast-forward, not a single squashed commit) referencing PR #33; package.json dependencies.openclerk-core equals exactly "^0.2.6"; npm ci exits 0 with no error; npm ls openclerk-core reports a resolved version satisfying ^0.2.6, and package-lock.json's resolved URL for openclerk-core starts with https://registry.npmjs.org/openclerk-core/; npm run build exits 0; npm test exits 0 with all suites passing; CI status for the merge commit on main is green (via gh, or via Task 1's resume confirmation if gh is unavailable).
  </acceptance_criteria>
  <verify>
    <automated>npm ci && npm run build && npm test</automated>
  </verify>
  <done>Merge commit confirmed on main with 4 preserved PR commits; openclerk-core ^0.2.6 resolves cleanly from the public npm registry per package-lock.json; production build and full Jest suite both pass; CI green on the merge commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify hallucination-check Core Value guard preserved post-merge (HIGH-severity threat T-01-02)</name>
  <files>src/taskpane/word.ts</files>
  <read_first>src/taskpane/word.ts (read the actual post-merge file -- do not rely on the pre-merge excerpt already summarized in 01-PATTERNS.md); .planning/phases/01-openclerk-core-dependency-cleanup/01-PATTERNS.md (PR #33 diff excerpts for the hallucination-check migration and the nameMismatch rendering branch); .claude/CLAUDE.md (Core Value statement)</read_first>
  <action>
    Grep src/taskpane/word.ts for: an import of checkCitationsForHallucinations and HallucinationCheckResult from "openclerk-core"; a call site invoking checkCitationsForHallucinations(candidates, selectedProviders) (or equivalent argument names as merged); the nameMismatch rendering branch containing the literal substring "Possible hallucination" and referencing result.nameMismatch.provider and result.nameMismatch.foundCaseName. Also grep the whole src/ tree for the old local per-citation type name "HallucinationResult" (distinct from the new "HallucinationCheckResult") to confirm the old hand-rolled type and its loop were fully removed, not left duplicated alongside the new shared implementation. Cross-check the call site sits in the same hallucination-check workflow described in 01-PATTERNS.md, not moved or duplicated elsewhere in the file.
    <!-- planner-discipline-allow: checkCitationsForHallucinations -->
    <!-- planner-discipline-allow: nameMismatch -->
    <!-- planner-discipline-allow: Possible hallucination -->
    <!-- planner-discipline-allow: HallucinationResult -->
  </action>
  <acceptance_criteria>
    grep -c 'checkCitationsForHallucinations' src/taskpane/word.ts is at least 2 (import plus call site); grep -c 'nameMismatch' src/taskpane/word.ts is at least 2 (type/result usage plus rendering branch); grep -c 'Possible hallucination' src/taskpane/word.ts equals 1 (rendering string present verbatim, not altered); grep -rc 'HallucinationResult' src/taskpane/word.ts (the old local type name) equals 0, confirming no orphaned hand-rolled hallucination type/loop remains duplicated alongside openclerk-core's HallucinationCheckResult.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "checkCitationsForHallucinations\|nameMismatch\|Possible hallucination" src/taskpane/word.ts</automated>
  </verify>
  <done>Post-merge src/taskpane/word.ts calls openclerk-core's checkCitationsForHallucinations and renders the nameMismatch guard verbatim; no orphaned old hand-rolled hallucination-loop or type remains -- the Core Value guard is confirmed preserved, not weakened, satisfying T-01-02's mitigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules / package-lock.json | Third-party (same-org sibling) package content crosses into the build here |
| GitHub PR branch -> main | Human-approved code-change boundary; merge strategy and CI-gate status determine what actually lands on main |
| openclerk-core checkCitationsForHallucinations() result -> task-pane DOM rendering | Verification-result trust boundary -- a "no name mismatch" result must never be silently upgraded to "verified" in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | package.json / package-lock.json openclerk-core dependency resolution | high | mitigate | Task 2 verifies package.json pins ^0.2.6 and package-lock.json's resolved field for openclerk-core points at registry.npmjs.org (not a git/tarball/local override), and that npm ls openclerk-core reports a version satisfying ^0.2.6 -- never trust package.json's semver range alone without confirming the lockfile-resolved version. |
| T-01-02 | Tampering | src/taskpane/word.ts hallucination-check call path (checkCitationsForHallucinations / nameMismatch) | high | mitigate | Task 3 greps the post-merge word.ts to confirm the checkCitationsForHallucinations call and the verbatim nameMismatch rendering branch are present, and that the old hand-rolled per-citation loop/type is fully removed. This is the project's literal Core Value guard (never report a fabricated citation as "verified"); any regression here blocks phase completion per security_block_on=high. |
| T-01-03 | Tampering | GitHub PR #33 merge process (merge strategy + CI-gate bypass risk) | medium | mitigate | Task 1's checkpoint instructs merge-commit-only strategy (D-01) and green-CI-only merge; Task 2 verifies the resulting merge commit itself (not just the pre-merge PR tip) has green CI. |
| T-01-SC | Tampering | openclerk-core npm package supply-chain trust | high | mitigate | openclerk-core is a same-org sibling package (OpenClerkProject/openclerk-core, extracted from this repo, already reviewed during onboarding per PROJECT.md Key Decisions) rather than a newly-introduced third-party install -- legitimacy is not [ASSUMED]/[SUS] by provenance, but Task 2 still confirms the resolved package pulls from the public npm registry, not a private/typosquat/local source, before CORE-01 is treated as complete. |
</threat_model>

<verification>
Post-merge, main branch has: a merge commit for PR #33 with 4 discrete original commits reachable;
package.json dependencies.openclerk-core = ^0.2.6, resolved from the public npm registry per
package-lock.json; `npm ci`, `npm run build`, and `npm test` all exit 0; src/taskpane/word.ts's
hallucination-check migration (checkCitationsForHallucinations, nameMismatch rendering) is present
verbatim with no orphaned old implementation left duplicated alongside it.
</verification>

<success_criteria>
CORE-01 is satisfied: PR #33 is merged to main with CI green (roadmap success criteria #1), npm
install succeeds cleanly against openclerk-core ^0.2.6 with no allow-scripts failure (roadmap success
criteria #2), and the Core Value hallucination-check guard is confirmed intact, not weakened, by the
merge.
</success_criteria>

<output>
Create `.planning/phases/01-openclerk-core-dependency-cleanup/01-01-SUMMARY.md` when done
</output>
